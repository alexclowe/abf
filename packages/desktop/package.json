{
  "name": "@abf/desktop",
  "version": "1.0.0",
  "private": true,
  "description": "ABF Desktop App — native wrapper for the ABF dashboard and runtime",
  "scripts": {
    "dev": "tauri dev",
    "build": "echo 'Tauri desktop app - run: pnpm tauri build'",
    "icons": "node scripts/generate-icons.mjs",
    "tauri": "tauri"
  },
  "dependencies": {
    "@abf/core": "workspace:*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
